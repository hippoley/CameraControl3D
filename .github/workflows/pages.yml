name: Deploy CameraControl3D to GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout full project
        uses: actions/checkout@v4

      - name: Verify project
        run: |
          set -euo pipefail
          test -f index.html
          test -f styles.css
          test -f app.js
          cp app.js /tmp/app.mjs
          node --check /tmp/app.mjs

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload full static project
        uses: actions/upload-pages-artifact@v3
        with:
          path: .

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
